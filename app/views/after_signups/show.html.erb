<%= simple_form_for [@user_city] do |f| %>
  <%= f.input :title, :collection => %w[Asterdam Istanbul Berlin Paris] %>
  <%= f.simple_fields_for [@user_interest] do |c| %>
    <%= c.association :interest %>
  <% end %>
  <%= f.submit 'Create and Add Another', name: 'create_and_add'%>

<% end %>

